export type PostInputType = {
  title: string;
  shortDescription: string;
  content: string;
  blogId: string;
};

export type PostUpdateType = {
  title: string;
  shortDescription: string;
  content: string;
  blogId: string;
};
